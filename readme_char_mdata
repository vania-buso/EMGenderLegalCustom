"chart_mdata" --> the count proceeds per charter. I.e., one entry = one charter.
id, cont, sub_cont, coll, rep, orig, authen, compl, issue_aut, issue_scr, issue_date, f_agent

char_id = charter database identification number = individual number that identifies a specific charter within the database created.
The id is composed of the ISO 3166 country Alpha-2 code and the entry number in the database. The country
code follows the CHLA division based on collection. E.g., IT1 is the id for the first Italian charter entered in the
database.
  Type() = string
  
cont = context = case study context taken into consideration. Options: regnum italiae | bavaria
  Type() = string
  
sub_cont = sub-context = case study context taken into consideration. Options: piacenza | lucca | freising
  Type() = string
  
coll = collection = the original collection the item was part of.
  Type() = string
  
rep = repository = the collection, volume or book from which the charter has been analyzed for this study.
  Type() = string
  
auth = authenticity = defines whether the charter is authentic. Options: authentic | forgery | mix.
  Type() = string 
  
copy = copy = defines whether the charter is a copy of another document. Options: copy | original | mix.
  Type() = string

issue_aut = issuing authority = authority invoked by the scribe in issuing the charters.
  Type () = string
  
issue_scr = issuing scribe = the person drafting the document. It can include name, title or both
  Type() = string
  
issue_date = issuing date = the date in which the document was drafted, reported in standard ISO 8601 (YYYY-MM-DD or its truncated and/or incomplete forms).
  Type() = datetime 
  
 date_sys = dating system = the date in which the document was drafted as reported verbatim from the doument.
  Type() = string
  
f_agent = identifies whether the charter mentions at least one female-sexed legal agent. This includes
groups of female agents (e.g., female religious institutions). The sexing of the agent
is based on the morphology of the name taken into consideration. This variable is not determined by the gender of the agent, but by its grammatical sexing.In cases where the attribution was unclear from the name and collateral factors, male | female was maintained. Options: male | female | (male | female)
  Type () = string
